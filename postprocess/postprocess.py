def inference_to_df(model, data):
    # model_list = glob.glob('trained_model/*.h5')
    # model_name = max(model_list, key=os.path.getctime)
    # model = k.models.load_model(model_name)
    predicted = []





    return predicted
